/**
 * 存在localstorage中的token的key
 */
export const TOKEN_KEY_IN_LOCAL_STORAGE = "token"

/**
 * 请求头 / 响应头中的token的key
 */
export const TOKEN_KEY_IN_HEADER = "Authorization"

/**
 * 请求头 / 响应头中的token的前缀
 */
export const TOKEN_KEY_IN_HEADER_PREFIX = "Bearer "

